export type Point = {
    x: number,
    y: number,
}

export type Drill = {
    x: number,
    y: number,
    size: number,
}

export type Profile = Point[];

export type Job = {
    title: string
    width: number,
    height: number,
}